[default]
  credential_process = sh -c "op item get 'AWS CLI Credentials' --format json --fields label=aws_access_key_id,label=aws_secret_access_key | jq '. | reduce .[] as $i ({}; .[$i.label] = $i.value) | {Version:1, AccessKeyId:.aws_access_key_id, SecretAccessKey:.aws_secret_access_key}'"